extends layout

block content
  h1= title
  p Welcome to #{title}
  if user
    p Hello, #{user.username}
    a(href="/logout") Logout
  else
    a(href="/login") Login
    #block.block
    button#scaling-button 拡大縮小
    button#moving-button 左右移動
  div#root